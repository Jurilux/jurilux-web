# Copier vers deploy/.env et renseigner. Ne jamais committer le .env réel.
SITE_ADDRESS=lexkyc.example.lu
POSTGRES_PASSWORD=
APP_DB_PASSWORD=
# openssl rand -hex 32 — en production : via KMS/secret manager
APP_ENC_KEY=
SCHEDULER=on
